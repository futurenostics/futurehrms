/**
 * Cross-app UI primitives.
 *
 * For now the only consumer is `apps/web`, which keeps its shadcn
 * primitives locally (per shadcn's "own, don't vendor" model). If a
 * future surface (e.g. an admin app, a public marketing site) needs to
 * share components, they migrate here.
 */
export {};
