// @ts-check
/**
 * Legacy files allowed to use Tailwind default utilities during the
 * Foundation Reset. The full violation count and remediation order
 * live in `docs/RESET_LINT_FAILURES.md`.
 *
 * RULES OF ENGAGEMENT
 *   1. Adding a file here is allowed ONLY for code that pre-dates the
 *      Sub-phase A lockdown commit. New code must use fn-* utilities.
 *   2. Every file in this list is a tracked deficit. The goal during
 *      Sub-phase D is to remove entries, not add them.
 *   3. When you remediate a file, delete its entry below AND run
 *      `pnpm --filter @futurenostics/web lint` to confirm it passes.
 *
 * Paths are relative to `apps/web/`, the cwd of the eslint config that
 * consumes this list (apps/web/eslint.config.mjs).
 *
 * Generated by A.6 lint sweep — 45 files, 1097 fn-tokens violations.
 */
export const legacySkipList = [
  // 'app/(app)/dashboard/page.tsx' — remediated in Sub-phase D.
  // 'app/(app)/employees/[id]/edit/page.tsx' — remediated in Sub-phase D.
  // 'app/(app)/employees/[id]/page.tsx' — remediated in Sub-phase D.
  // 'app/(app)/employees/import/page.tsx' — remediated in Sub-phase D.
  // 'app/(app)/employees/new/page.tsx' — remediated in Sub-phase D.
  // 'app/(app)/employees/org/page.tsx' — remediated in Sub-phase D.
  // 'app/(app)/employees/page.tsx' — remediated in Sub-phase D.
  // 'app/(app)/layout.tsx' — remediated in Sub-phase D.
  // 'app/(auth)/layout.tsx' — remediated in Sub-phase D.
  // 'app/(auth)/login/forgot-password-dialog.tsx' — remediated in Sub-phase D.
  // 'app/(auth)/login/login-card.tsx' — remediated in Sub-phase D.
  // 'components/brand/logo.tsx' — remediated in Sub-phase D.
  // 'components/employees/dialogs/change-manager-dialog.tsx' — remediated in Sub-phase D.
  // 'components/employees/dialogs/change-salary-dialog.tsx' — remediated in Sub-phase D.
  // 'components/employees/dialogs/change-status-dialog.tsx' — remediated in Sub-phase D.
  // 'components/employees/employee-avatar.tsx' — remediated in Sub-phase D.
  // 'components/employees/employee-form.tsx' — remediated in Sub-phase D.
  // 'components/employees/kpi-strip.tsx' — remediated in Sub-phase D.
  // 'components/employees/profile-header.tsx' — remediated in Sub-phase D.
  // 'components/employees/status-pill.tsx' — remediated in Sub-phase D.
  // 'components/employees/widgets/total-employees-widget.tsx' — remediated in Sub-phase D.
  // 'components/shell/app-shell.tsx' — remediated in Sub-phase D.
  // 'components/shell/brand-mark.tsx' — remediated in Sub-phase D.
  // 'components/shell/currency-toggle.tsx' — remediated in Sub-phase D.
  // 'components/shell/sidebar.tsx' — remediated in Sub-phase D.
  // 'components/shell/theme-toggle.tsx' — remediated in Sub-phase D.
  // 'components/shell/topbar.tsx' — remediated in Sub-phase D.
  // 'components/shell/user-menu.tsx' — remediated in Sub-phase D.
  // 'components/ui/avatar.tsx' — remediated in Sub-phase B Tier 3.
  // 'components/ui/badge.tsx' — remediated in Sub-phase B Tier 3.
  // 'components/ui/button.tsx' — remediated in Sub-phase B Tier 2.
  // 'components/ui/card.tsx' — remediated in Sub-phase B Tier 4.
  // 'components/ui/checkbox.tsx' — remediated in Sub-phase B Tier 2.
  // 'components/ui/dialog.tsx' — remediated in Sub-phase B Tier 4.
  // 'components/ui/dropdown-menu.tsx' — remediated in Sub-phase B Tier 4.
  'components/ui/form.tsx',
  // 'components/ui/input.tsx' — remediated in Sub-phase B Tier 2.
  // 'components/ui/label.tsx' — remediated in Sub-phase B Tier 1.
  // 'components/ui/popover.tsx' — remediated in Sub-phase B Tier 4.
  // 'components/ui/select.tsx' — remediated in Sub-phase B Tier 2.
  // 'components/ui/separator.tsx' — remediated in Sub-phase B Tier 1.
  // 'components/ui/sheet.tsx' — remediated in Sub-phase B Tier 4.
  'components/ui/table.tsx',
  'components/ui/tabs.tsx',
  // 'components/ui/tooltip.tsx' — remediated in Sub-phase B Tier 3.
];

export default legacySkipList;
