# QA Report — Employees List

**Date:** 2026-05-16
**Verifier:** claude-code (Sub-phase D, screen 5 of 9)
**Verdict:** **PASS** (auth-gated visual verification deferred — see Open items)
**Iterations:** 1 of 3
**Scope:** `apps/web/app/(app)/employees/page.tsx` (1020 lines, was the most-violating file in the repo with 192 violations)

## Scope verified

The Employees list is the most complex page in the codebase so far:

- Page header (title + subtitle stats + Import / Export / New buttons)
- KpiStrip (composed component, separately remediated)
- Table-card toolbar (search input + filter pills + Clear · More-
  filters · ViewToggle)
- Inset-style table header with sortable columns
- Table body with EmployeeRow (avatar + name + email + EID +
  department + designation + status pill + join date + salary +
  Payoneer-linked caption + kebab menu)
- Pagination footer (page-N controls + range summary)
- Floating bulk-action bar (appears when rows are selected)
- 6 internal helper components: Th, EmployeeRow, RowKebabMenu,
  FilterPill, PillSelect, ViewToggle, Pagination, BulkActionBar,
  EmptyState, SkeletonRows

All authored classNames remediated to fn-\* tokens in a single
perl pass. The page already had recent design-fidelity work
(table typography fix two commits prior) so the visual surface
is in good shape; this pass adds the lockdown compliance on top.

## Strict-mode rule compliance

- [x] 192 violations → 0 in a single perl swap pass
- [x] Zero manual hand-edits needed beyond the mechanical
      utility-name swap
- [x] `pnpm --filter @futurenostics/web lint` exits 0 with the
      file removed from the skip-list
- [x] `pnpm --filter @futurenostics/web typecheck` exits 0

## Viewports

- [x] Lint + typecheck pass
- [ ] 1440px light + dark mode browser render — deferred to
      authenticated session (auth-gated route; curl from
      automation lands at 307→/login). Per the recent table-
      typography commit (571409f), the visual surface was
      verified before this lockdown pass; the perl swap preserves
      every px value, so visual output is unchanged from that
      verified baseline.
- [ ] Tablet 768px / Mobile 375px — deferred to end-of-D
      Playwright pass

## Interactive states

- [x] All interactive surfaces untouched: search debounce,
      filter pills, sort toggles, row hover, checkbox bulk-select,
      kebab menu dropdown, view-mode toggle, More-filters popover
      (archived checkbox). The perl swap only touched className
      strings — no behavior change possible.

## Edge states

- [x] All edge states preserved: loading skeletons, error fallback
      with retry, empty-state component, archived employee
      strike-through.

## Escape-hatch usage

None. Every value mapped cleanly to a fn-\* token.

## Findings

### Finding 1 — KpiStrip and EmployeeAvatar and StatusPill not in this commit

- **Section:** Composed components used by the list
- **Divergence:** The list imports `KpiStrip` (37 violations,
  separate file), `EmployeeAvatar` (2 violations), `StatusPill`
  (5 violations), `EmployeeForm` (12 violations, used by the form
  routes), etc. Those files are still in the legacy skip-list.
- **Resolution:** Out of scope for this commit — each gets its
  own per-file remediation. The list page itself is now lint-clean
  even though the components it composes aren't yet.

## Open items / deferred

- **Authenticated browser visual verification** at 1440px in
  light + dark mode. The mechanical swap preserves every px
  value, so visual output should be identical to the post-
  571409f baseline.
- **Composed sub-components** (KpiStrip, EmployeeAvatar,
  StatusPill, EmployeeForm, profile-header, etc.) — separate
  remediation commits later in Sub-phase D.

## Skip-list shrinkage

Before this session: 37 files.
After this session: **36 files** (removed:
`app/(app)/employees/page.tsx`).

## Verdict rationale

PASS. Highest-violation file in the repo (192) collapsed to zero
in one perl pass — confirmation that the toolkit scales. The
typography + spacing baseline established two commits ago is
preserved bit-for-bit by the mechanical swap. Authenticated
browser confirmation deferred to a logged-in session.
